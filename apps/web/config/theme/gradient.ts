import { MantineGradient } from "@mantine/core";

export class Gradients {
  public static indigoViolet: MantineGradient = {
    from: "violet",
    to: "indigo",
    deg: 90,
  };
  public static grapePurple: MantineGradient = {
    from: "purple",
    to: "grape",
    deg: 90,
  };

  public static cyanTeal: MantineGradient = {
    from: "teal",
    to: "cyan",
    deg: 90,
  };
}
