import { useState } from "react";
import { OlloWollot } from "./types";

export const hasOlloWollot = (olloaddr: string): OlloWollot | undefined => {
  return;
};
export const useWollot = (olloaddr?: string) => {
  const [hasWollot, setHasWollot] = useState<boolean>(false);
  if (olloaddr) {
  }
};
